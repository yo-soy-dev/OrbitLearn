import React from "react";
import { PricingTable } from "@clerk/nextjs";

const Subscription = () => {
  return (
    <main className="min-h-screen flex justify-center items-center bg-gray-50 p-10">
      <PricingTable
      />
    </main>
  );
};

export default Subscription;
